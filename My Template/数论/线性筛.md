# 线性筛求质数模板

```cpp
for (int i = 2; i <= n; i++) {
    if (!vis[i]) prime.push_back(i);
    for (int j : prime) {
        if (i * j > n) break;
        vis[i * j] = 1;
        if (i % j == 0) break;
    } 
}
```