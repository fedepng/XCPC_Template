# 快速幂模板

```cpp
int power(int y, int x) {
    if (x == 0) return 1;
    int sum = 1;
    while (x) {
        if (x & 1) sum = sum * y % mod;
        y = y * y % mod;
        x >>= 1;
    }
    return sum;
}
```